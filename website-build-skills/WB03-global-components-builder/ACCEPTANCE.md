# Acceptance
PASS requires package/schema/template/example validation, architecture/design-token alignment, valid CTA/forms, reusable-pattern safety, explicit production approvals, semantic mutation rejection, compile/secret/inventory/ZIP integrity checks.
