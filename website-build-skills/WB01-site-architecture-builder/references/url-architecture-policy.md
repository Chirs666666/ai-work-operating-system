# Url Architecture Policy

Proposed URLs must be unique, stable, readable, and aligned to page ownership. Existing live URLs in rebuild/migration work are preservation inputs, not disposable placeholders.
