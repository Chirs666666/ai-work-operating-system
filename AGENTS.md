# AI Work Operating System
Use `seo-workflow-system` as the default orchestrator for SEO + website projects.
Route website engineering through `website-build-layer`.
Discover existing artifacts before creating new work; prefer valid REUSE or VERIFY.
Never route standalone Skill 14; resolve it to Skill 12.
Technical SEO → 12. Indexing → 19. Ranking validation → 20.
Production-changing actions require explicit approval.
Preserve UNKNOWN/null and evidence provenance; never fabricate metrics, evidence or credentials.
