# Placement Rules
V1 placement types: `BODY_CONTEXTUAL`, `NAVIGATION`, `BREADCRUMB`, `RELATED_CONTENT`, `CTA`, `TABLE`, `FAQ`, `CARD`, `FOOTER`. Sitewide `NAVIGATION`/`FOOTER` changes are recommendations only and require approval before execution.
