# Boundaries and Ownership
25 decides which URL owns a topic; 08 decides how other pages support that URL; 07 executes approved WordPress changes; 12 repairs technical link faults. `OWNERSHIP_CONFLICT` returns to 25 and blocks dependent link decisions. 08 must not directly modify WordPress.
