# Anchor Strategy
Intent-aware anchor types: `EXACT_DESCRIPTIVE`, `PARTIAL_DESCRIPTIVE`, `ENTITY_PRODUCT`, `CONTEXTUAL`, `ACTION`. Anchors must accurately describe the target and read naturally. No fixed exact-match/partial-match ratio. Repeated runs should preserve a good anchor rather than mechanically rewrite it.
