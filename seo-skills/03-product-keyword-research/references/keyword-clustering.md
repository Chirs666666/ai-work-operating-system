# Keyword Clustering
Use semantic similarity, intent, SERP overlap where available, buyer problem/use case and product/application relationship. Keep distinct intents separate. Uncertain membership remains uncertain. A cluster never assigns the final URL.
