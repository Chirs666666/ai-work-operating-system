# 09 SEO Metadata
Evidence-backed SEO Title and Meta Description decision and optimization skill.

Modes: GENERATE, AUDIT_PAGE, OPTIMIZE, PERFORMANCE_DIAGNOSIS, SITE_AUDIT, VERIFY.

Boundaries: 01 owns truth; 25 owns keyword/topic-to-URL ownership; 09 owns metadata decisions and H1 alignment review; 07 owns WordPress/SEO-plugin writes. Provider credentials are runtime-only.

Validate:
`python scripts/validate_package.py .`
`python scripts/validate_examples.py examples`
