# Verification and Reuse
VERIFY states: VERIFIED, PARTIAL, MISMATCH, MISSING, CONFLICT. Compare approved handoff with configured/read-back metadata, not Google's eventual SERP rendering. Healthy existing metadata may return REUSE without candidate generation.
